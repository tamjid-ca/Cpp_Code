#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double num;

    cout << "Enter a decimal number: ";
    cin >> num;

    cout << "Rounded number is: " << round(num);
    return 0;
}